<%= form_for(@fable) do |f| %>
<% if @fable.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@fable.errors.count, "error") %> prohibited this fable from being saved:</h2>

	<ul>
		<% @fable.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="field">
	<%= f.label :date %>
	<br>
	<%= f.text_field :date, :class => "datepicker" %>
</div>
<div class="actions">
	<%= f.submit %>
</div>
<% end %>


<script>$(".datepicker").datepicker({dateFormat: 'yy-mm-dd'});</script>
